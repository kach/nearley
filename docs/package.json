{
  "name": "nearley-docs",
  "version": "0.0.1",
  "main": "make.js",
  "scripts": {
    "make": "rm *.html && node make.js && cp index.html ../",
    "start": "http-server -c5 ../"
  },
  "dependencies": {
    "handlebars": "^4.0.10",
    "metalsmith": "^2.3.0",
    "metalsmith-headings": "^0.2.0",
    "metalsmith-layouts": "^1.8.1",
    "metalsmith-markdown": "^0.2.1",
    "metalsmith-paths": "^3.0.1"
  },
  "devDependencies": {
    "http-server": "^0.10.0"
  }
}
